#!/bin/sh

/usr/bin/php /app/bin/substitutePlaceholders /etc/nginx/sites-enabled/default

echo ""
echo "#############################################"
echo "Starting WebFrontend"

php-fpm83 -D && nginx -g "daemon off;"
