#!/bin/sh

/bin/php /app/bin/substitutePlaceholders /etc/nginx/sites-enabled/default

echo ""
echo "#############################################"
echo "Starting WebFrontend"

php-fpm8.3 -D && nginx -g "daemon off;"
